# Обновление безопасности и конфиденциальности

## Что было сделано

### 1. Новый раздел "Конфиденциальность"
- ✅ Создан компонент `PrivacySettingsView.tsx`
- ✅ Настройки видимости:
  - Время последнего посещения
  - Фото профиля
  - О себе
  - Номер телефона
- ✅ Три уровня приватности: Все / Мои контакты / Никто

### 2. Улучшенный раздел "Безопасность"
- ✅ Код-пароль (существующая функция)
- ✅ Изменение пароля аккаунта
  - Проверка текущего пароля
  - Установка нового пароля
  - Валидация (минимум 6 символов)
- ✅ Активные сессии
  - Просмотр всех устройств
  - Завершение отдельных сессий
  - Завершение всех сессий кроме текущей

### 3. Исправлен статус "в сети"
- ✅ Раскомментирован код обновления статуса в `ChatContext.tsx`
- ✅ Статус обновляется при:
  - Входе в приложение → "в сети"
  - Переключении вкладки → "в сети" / "не в сети"
  - Закрытии приложения → "не в сети"
  - Выходе из аккаунта → "не в сети"
- ✅ Отображение времени последнего посещения для офлайн пользователей

## Структура файлов

```
components/
├── SecurityView.tsx          # Обновлен с новыми функциями
├── PrivacySettingsView.tsx   # Новый компонент
└── SettingsView.tsx          # Обновлены ссылки на разделы

context/
└── ChatContext.tsx           # Включено обновление статуса

types/
└── index.ts                  # Добавлен 'privacy-settings' в ViewState

app/
└── page.tsx                  # Добавлен роутинг для нового view
```

## Как использовать

### Настройки конфиденциальности
1. Откройте Настройки
2. Выберите "Конфиденциальность"
3. Настройте видимость для каждого параметра

### Безопасность
1. Откройте Настройки
2. Выберите "Безопасность"
3. Доступные функции:
   - Код-пароль для входа
   - Изменение пароля аккаунта
   - Управление активными сессиями

## Технические детали

### Обновление статуса пользователя
```typescript
// При входе
await updateDoc(doc(db, 'users', currentUser.uid), { 
  status: 'online', 
  lastSeen: serverTimestamp() 
});

// При выходе/закрытии
await updateDoc(doc(db, 'users', currentUser.uid), { 
  status: 'offline', 
  lastSeen: serverTimestamp() 
});
```

### Смена пароля
```typescript
// Реаутентификация
const credential = EmailAuthProvider.credential(email, currentPassword);
await reauthenticateWithCredential(user, credential);

// Обновление пароля
await updatePassword(user, newPassword);
```

## Что дальше

### Возможные улучшения:
- [ ] Сохранение настроек конфиденциальности в Firestore
- [ ] Реальное отслеживание активных сессий через Firebase
- [ ] Двухфакторная аутентификация
- [ ] История входов
- [ ] Уведомления о новых входах

## Примечания

- Все изменения совместимы с существующими правилами Firestore
- Статус пользователя теперь корректно обновляется в реальном времени
- Интерфейс следует дизайну Telegram
